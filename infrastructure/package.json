{
  "name": "56k-slides-infra",
  "engines": {
    "node": "22.19.0"
  },
  "devDependencies": {
    "@types/node": ">=22.19.0",
    "typescript": ">=5.9.2",
    "@biomejs/biome": "^2.2.3"
  },
  "dependencies": {
    "@pulumi/aws": ">=7.7.0",
    "@pulumi/pulumi": ">=3.193.0"
  },
  "scripts": {
    "dev": "echo 'No dev mode for infra, use pulumi up instead'",
    "build": "tsc --noEmit",
    "lint": "biome lint .",
    "format": "biome format . --write",
    "check": "biome check ."
  }
}
